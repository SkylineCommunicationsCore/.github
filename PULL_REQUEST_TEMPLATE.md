<!--
  Default Pull Request template for the SkylineCommunicationsCore organization.
  Lives in the `.github` repo and is applied automatically to every repository
  that does not define its own template.

  Keep the "Release Notes & Tasks" table — it is parsed by the mandatory
  PR-validation check. The rest of the body is yours to describe the change.
-->

## Description

<!-- What does this PR change, and why? Add any context a reviewer needs. -->



## Release Notes & Tasks

<!--
  At least one Release Note (RN) and one task are required.
  - One row per RN -> task(s) grouping; add as many rows as needed.
  - Multiple RNs/tasks in the SAME row are comma-separated and treated as linked.
  - Dependabot PRs are the only exception: they need only an RN (task not required).

  Example:
  | RN12       | DCP35        |
  | RN12, RN13 | DCP35, DCP66 |
-->

| RN | Task |
|----|------|
|    |      |

<!--
  Versioning: the SemVer bump on merge is driven by a `Change-Type:*` LABEL on this PR
  (Change-Type:Minor | Change-Type:Major). Default is Patch, so only add a
  label for Minor or Major changes. Do not put the Change-Type in the description.

  Need a test build on dev/qa before merging? Add the `Create-Prerelease` label to this PR
  (works in both release modes). The automation tags a pre-release, removes the label, and
  comments which version is being made.
-->
